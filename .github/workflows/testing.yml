name: Maven Reusable Workflow
run-name: GitHub Action Maven Reusable Workflow
on:
  workflow_dispatch:
  workflow_call:
jobs:
  Test-Workflow:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout tools repo
      run: |
        git clone https://anujdevopslearn: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@github.com/anujdevopslearn/PythonTest test
    - name: Invoke Python script
      run: python -m unittest discover
      working-directory: test/example-py-unittest/
